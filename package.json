{
  "name": "home-finanzas",
  "version": "2.0.0",
  "description": "App de finanzas en pareja – backend Firebase Firestore",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "migrate:sqlite": "node scripts/migrate-sqlite-to-firestore.js",
    "cleanup:sqlite": "node scripts/cleanup-sqlite.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
